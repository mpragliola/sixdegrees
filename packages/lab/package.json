{
  "name": "lab",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "sixdegrees": "workspace:*",
    "stopword": "^3.1.5"
  },
  "devDependencies": {
    "@types/stopword": "^2.0.3",
    "typescript": "^5.7.0",
    "vite": "^8.2.1"
  }
}
